# ── API ────────────────────────────────────────────────────
# Same-origin (default): leave as /api (requests go to same server)
# Cross-origin: set to full API URL (e.g., https://api.example.com)
VITE_API_URL=/api
